<!DOCTYPE html>
<html lang="en">
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/w3css.css">
    <%- include('../partials/dashboard/head.ejs') %>
    <style>
      .notifications-container{
        max-height: 80vh;
        overflow-y: auto;
      }
    </style>
</head>
<body>
    <main>
    <%- include('../partials/dashboard/sidebar.ejs') %>
    <section class="vastmint-dashboard-page">
    <%- include('../partials/header2.ejs') %>
    <%- include('../partials/dashboard/header.ejs') %>
       <p class="fs-4 fw-semi-bold">Your Notifications</p>
       <% if (response.status == "success") { %>
        <div class="notifications-container">
           <% if (response.notifications.length > 0) { %>
            <% for(notification of response.notifications) { %>
                <div class="vastmint-dark mb-4 w3-card grid-col-6 grid-col-md-6 grid-col-sm-12 w3-round-large me-2 " style="min-height:80px;cursor:pointer">
                    <div class="d-md-flex justify-content-between">
                        <div class="p-2">
                            <b class="mb-0"><%= notification.title %></b>
                            <p><%= notification.message %> </p>
                         </div>
                         <div class="p-2">
                            <p class="vastmint-text-light-700 m-0"><small><%= new Date(notification.createdAt).toISOString() %></small></p>
                         </div>
                    </div>
                  </div>
            <% } %>
           <% }else{ %>
            <div class="w3-center p-3 mx-auto" style="max-width: 500px;">
                <img src="/assets/nodata.svg" style="max-width:300px" alt="">
                <p class="fw-semibold mb-0" style="font-size: 30px;">No Notifications</p>
                <p class="mb-0">You have not recieved any notification</p><br>
            </div>
           <% } %>
           </div>
       <% }else{ %>
        <div class="w3-center p-3 mx-auto" style="max-width: 500px;">
            <img src="/assets/500.svg" alt="">
            <p class="fw-semibold mb-0" style="font-size: 30px;">Server Error</p>
            <p class="mb-0">Sorry could not get this content due some server error Please try again later while we sort this issue out</p><br>
            <button onclick="window.location.reload()" href="/" class="mt-0 btn vastmint-primary-400 vastmint-text-white" style="padding:10px 16px;border-radius:30px">Refresh</>
        </div>
       <% }%>
    </section>
</main>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
</html>